<div class="content-image-2-col">
  <div class="bd">
    <slot />
  </div>
</div>

<style>
  .content-image-2-col {
    container-type: inline-size;
  }

  .content-image-2-col .bd {
    display: flex;
    flex-direction: column;
    gap: var(--space-8);
  }

  @container (width > 966px) {
    .content-image-2-col .bd {
      flex-direction: row;
      margin-top: var(--space-8);
    }
  }
</style>
