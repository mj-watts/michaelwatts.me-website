<a href="/" class="avatar">
  <span role="heading" aria-level="1" class="avatar__heading">
    <span class="avatar__heading--name">Michael Watts</span>
    <span class="avatar__heading--title">Software Engineer</span>
  </span>
</a>

<script>
  import { animate } from "animejs";

  if (
    "IntersectionObserver" in window &&
    "IntersectionObserverEntry" in window &&
    "intersectionRatio" in window.IntersectionObserverEntry.prototype
  ) {
    const section = document.querySelector("#hero");

    const observer = new IntersectionObserver(
      (entries, observer) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            animate(".avatar__heading--title", {
              translateY: -100,
              duration: 1000,
              ease: "outQuart",
            });
          } else {
            animate(".avatar__heading--title", {
              translateY: 0,
              delay: 200,
              duration: 1000,
              ease: "outQuart",
            });
          }
        });
      },
      { threshold: 0.9 }
    );

    if (section) {
      observer.observe(section);
    }
  }
</script>

<style>
  .avatar {
    --heading-display: none;
    --title-display: none;
    --size: 48px;
  }

  @media screen and (min-width: 768px) {
    .avatar {
      --heading-display: flex;
    }
  }

  @media screen and (min-width: 950px) {
    .avatar {
      --title-display: block;
    }
  }

  .avatar {
    display: flex;
    gap: var(--space-3);
    align-items: center;
    justify-content: start;
    margin: 0;
    line-height: normal;
  }

  .avatar__icon-wrapper {
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: var(--color-onPrimary);
    height: var(--size);
    width: var(--size);
    border-radius: var(--border-radius-sm);
    color: var(--color-primary);
  }

  .avatar__icon {
    height: 26px;
    width: 26px;
    fill: #eace09;
  }

  .avatar__heading {
    color: var(--color-onPrimary);
    display: var(--heading-display);
    gap: var(--space-2);
    font-family: var(--header-font);
    font-size: var(--font-size-lg);
    font-weight: bold;
    transform: scale(1.5);
    transform-origin: left center;
    overflow: hidden;
  }

  .avatar__heading--name,
  .avatar__heading--title {
    display: block;
    isolation: isolate;
    z-index: 2;
  }

  .avatar__heading--title {
    display: var(--title-display);
    color: var(--color-emphasis);
    transform: translateY(-100px);
    transform-origin: left center;
    isolation: isolate;
    z-index: 1;
  }

  .avatar__icon-expression--none,
  .avatar__icon-expression--love {
    transition: opacity 200ms ease-in-out;
  }

  .avatar:hover .avatar__icon-expression--none,
  .avatar[inview-work] .avatar__icon-expression--none {
    opacity: 0;
  }

  .avatar__icon-expression--grimace {
    opacity: 0;
  }
</style>
