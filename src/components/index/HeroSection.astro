<h1 class="title">
  Michael Watts
  <span class="title__partial">Software Engineer</span>
</h1>

<style>
  .title {
    font-size: clamp(
      var(--font-size-lg),
      calc(10vw + 1rem),
      var(--font-size-4xl)
    );
    text-align: center;
    color: var(--color-secondary);
    line-height: 1;
  }

  .title__partial {
    display: block;
    color: var(--color-onPrimary);
  }
</style>
