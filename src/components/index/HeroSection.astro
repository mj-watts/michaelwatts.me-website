<h1>
  Michael Watts
  <span>Software Engineer</span>
</h1>

<style>
  h1 {
    font-size: clamp(
      var(--font-size-lg),
      calc(10vw + 1rem),
      var(--font-size-4xl)
    );
    text-align: center;
    color: var(--color-secondary);
    line-height: 1;
  }

  h1 span {
    display: block;
    color: var(--color-onPrimary);
  }
</style>
