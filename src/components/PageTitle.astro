<h1 class="page-title">
  <slot />
</h1>

<style>
  .page-title {
    margin: var(--space-8) 0;
    line-height: 1.2;
    font-family: var(--body-font);
    font-variation-settings: var(--font-bold);
    font-weight: 700;
    text-wrap: pretty;
  }
</style>
